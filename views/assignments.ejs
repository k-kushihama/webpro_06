<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/assignments.css">
    <title>課題管理システム</title>
</head>
<body>
    <div class="nakami">
        <div class="header">
            <h1>課題一覧</h1>
            <a href="/assignments/create" class="createbotan">新規登録</a>
        </div>

        <% if (data.length === 0) { %>
            <div class="nanimonai">
                <p>現在，課題はありません．</p>
            </div>
        <% } else { %>
            <div class="list">
                <% data.forEach(item => { %>
                    <% 
                       const today = new Date();
                       const dead = new Date(item.deadline);
                       const diffDays = Math.ceil((dead - today) / (1000 * 60 * 60 * 24));
                       const isUrgent = diffDays <= 3 && diffDays >= 0; 
                    %>
                    <div style="border: 1px solid #ccc; margin-bottom: 10px; padding: 10px;">
                        <a href="/assignments/<%= item.id %>" style="text-decoration: none; color: black;">
                            <div class="card">
                                <span class="kyouka">[<%= item.subject %>]</span>
                                <span class="jyoutai">状態：<%= item.status %></span>
                            </div>
                            <div class="title"><%= item.title %></div>
                            <div class="deadline">
                                期限：<%= item.deadline %>
                                <% if(isUrgent && item.status !== '完了') { %> 
                                    <span class="kigen">（あと<%= diffDays %>日！）</span> 
                                <% } %>
                            </div>
                        </a>
                        <div class="actions">
                            <a href="/assignments/<%= item.id %>/edit" class="edit">編集する</a>
                            <a href="/assignments/<%= item.id %>/delete" class="delete">削除</a>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>
</body>
</html>
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/assignments.css">
    <title>編集 - 課題管理システム</title>
</head>
<body>
    <div class="nakami">
        <div class="header">
            <h1>課題の編集</h1>
        </div>

        <form action="/assignments/<%= item.id %>/update" method="POST">
            <div class="form">
                <label>科目名</label>
                <input type="text" name="subject" value="<%= item.subject %>" required>
            </div>

            <div class="form">
                <label>課題タイトル</label>
                <input type="text" name="title" value="<%= item.title %>" required>
            </div>

            <div class="form">
                <label>提出期限</label>
                <input type="date" name="deadline" value="<%= item.deadline %>" required>
            </div>

            <div class="form">
                <label>ステータス</label>
                <select name="status">
                    <option value="未着手" <%= item.status === '未着手' ? 'selected' : '' %>>未着手</option>
                    <option value="進行中" <%= item.status === '進行中' ? 'selected' : '' %>>進行中</option>
                    <option value="完了" <%= item.status === '完了' ? 'selected' : '' %>>完了</option>
                </select>
            </div>

            <div class="form">
                <label>詳細・メモ</label>
                <textarea name="desc" style="height: 120px;"><%= item.desc %></textarea>
            </div>
            
            <div class="botanarea">
                <button type="submit" class="createbotan submitbotan">更新を保存</button>
                <a href="/assignments/<%= item.id %>"><span class="editc">キャンセル</span></a>
            </div>
        </form>
    </div>
</body>
</html>